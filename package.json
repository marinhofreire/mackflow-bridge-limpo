{
    "name": "mackflow-bridge",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "@cloudflare/workers-types": "^4.0.0"
    }
}